// push = insere um elemento no array
// pop = remove um elemento no array      ...AMBOS no FIM DO ARRAY

let nomes = ['João', 'Maria', 'José'];
console.log('Antes do pop '  + nomes);

let elementoRemovido = nomes.pop();

console.log('ultimo nome ' + elementoRemovido);
console.log(nomes);


