// metodo vvem depois da variavel

let marca = 'nike';

console.log(typeof marca.toUpperCase()); // typeof diz o que a variável é no caso STRING
console.log(marca.toUpperCase()); // o método colocar todas as letras em maiúsculo.



let marca2 = 'ADIDAS';
console.log(typeof marca2.toLowerCase()); // typeof diz o que a variável é no caso STRING
console.log(marca2.toLowerCase()); // o método colocar todas as letras em maiúsculo.

console.log(typeof marca2.toLocaleLowerCase); // diz o que o método é. No caso uma function

//INVOCANDO PROPRIEDADE E MÉTODO
//string.propriedade
//string.metodo()